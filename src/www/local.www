#!/bin/bash

SCRIPT_DIR=$(cd `dirname $0` && pwd)
cd $SCRIPT_DIR

# Compress the tinyhttp binary
upx ./_install/tinyhttp

upx ./_install/cgi-bin/*.cgi

rsync -a ./_install/tinyhttp local/bin/

rsync -a ./_install/htdocs local/www
rsync -a ./_install/cgi-bin local/www
